# AnalyticsAI

Test project to build an analytics project using GenAI.

## Java Maven Setup

This repository now includes a standard Maven Java project layout:

- `src/main/java` for application source code
- `src/test/java` for test code
- `pom.xml` for project/build/test configuration

## Run

```bash
mvn test
```
